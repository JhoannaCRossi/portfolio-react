/* JSX */

//1*- expresiones solo entre llaves 
const name = 'jhonny'
//<h1>{name}</h1>

//2*-siempre cosas que devuelvan una expresion, nada de if (porq son declaraciones)
const element = <strong>Numero Aleatorio. {Math.random()}</strong>;

//2*- atributos de los componentes en camelCase
<button dataIcon='123'>button</button>

